# 统一各编辑器的基础风格，与 .prettierrc.json 保持一致
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

[*.md]
# Markdown 行尾空格有语义（硬换行），不裁剪
trim_trailing_whitespace = false

# 待办 / 已决策清单

## 待办(暂不做,验证后再说)

- [ ] **炫特效(验证后再加)**:动效定调"先朴素、验证后再加"。验证出有人持续打卡后,
      按下面的"分端降级策略"加重特效。第一版所有端只做最朴素动画,把核心闭环跑顺。
  - 分端降级策略(将来执行用):
    - 数字滚动(打卡灵魂):H5 用 react-spring;小程序手写 CSS+定时器。两端都自己掌控,不赌库能否在小程序跑。
    - 转折/庆祝特效:Lottie(H5)+ lottie-miniprogram(小程序)→ 跨端通用,设计师给 JSON 即可,首推。
    - 3D 亮点:react-three-fiber 只在 H5 做(如可旋转卡片/回本金币雨);小程序不做 3D(投入产出比极低)。
    - 滑动切卡:Taro 内置 swiper,跨端通用。
  - 硬约束备忘:小程序非浏览器(双线程、无 DOM),GSAP/Framer Motion 操作 DOM 部分、标准 Three.js 用不了;
    小程序主包 2MB 限制,动画资源别堆太多。
- [ ] **分享卡**:转折节点(回本、跌破整数、超额里程碑)生成可保存/分享的图片(canvas)。
      获客利器,但需额外做视觉。等"屏幕上的转折提示"被验证有人爱看后再做。
- [ ] **主动推送**:负反馈/涨价提醒的主动推送(隔几天弹一下)。杀伤力强但易招烦,
      且涉及小程序推送限额。第一版只做"打开时被动显示"。
- [ ] **多端同步**:接云数据库。数据模型已按"绑用户、可同步"设计(每条记录带 id),
      存储层已隔离成 4 个函数,平移时只改存储层。
- [ ] **底部 tab(总览页 / 我的页)**:首页先做单页"打卡优先",tab 结构等核心体验验证后再补。

## 已决策

- 两种卡都做:不限次年卡 / 有限次课包
- 回本基准:用户自填"心里一次值多少钱"
- 可补卡(限购买日~今天)、一天可多打卡
- 单条打卡记录可删除
- 首页:打卡优先(大数字 + 大按钮 + 左右滑切换多卡)
- 反馈四层:即时(打卡爽感)/ 状态(颜色+人话)/ 转折(屏幕提示)/ 负反馈(随天数递进)
- 负反馈随断卡天数递进升级:温和(3-6天)→ 给数字(7-14天)→ 扎心算日烧钱(15天+)
- 防沉没成本陷阱(A+C 组合):
  - A 沉默期止损:断卡超阈值(暂定 21 天)时,负反馈切换语气,从"快回来回本"
    改为"也许这卡本就不适合你,接受沉没成本、下次别续更省钱"
  - C 到期复盘:卡到期时给结论,用本次数据指导下次决策(如"这频率办月卡更划算,别再办年卡")
- 首次体验:空状态放一张可直接玩的「健身年卡」示例卡,点一下打卡看单价掉,
  先尝甜头再引导"添加自己的卡";表单能默认就默认(年卡有效期=购买日+1年)、
  非必填字段(心理价)给"可先不填"提示、字段按"答得出"的顺序排
- 少样本不给预测:前 7 天 / 前 3 次打卡内不显示预测,改为"再打几次就能预测真实成本"
- 计算边界兜底:除零、补卡越界、打卡超总次数、过期后不再预测等
- 数据导出/导入:过渡期安全网(云同步前),成本低价值高,这版顺手做
